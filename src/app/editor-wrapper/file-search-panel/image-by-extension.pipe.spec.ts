import { ImageByExtensionPipe } from './image-by-extension.pipe';

describe('ImageByExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageByExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});

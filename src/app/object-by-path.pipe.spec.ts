import { ObjectByPathPipe } from './object-by-path.pipe';

describe('ObjectByPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectByPathPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ObjectByProbablePathPipe } from './object-by-probable-path.pipe';

describe('ObjectByProbablePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectByProbablePathPipe();
    expect(pipe).toBeTruthy();
  });
});
